# CRAVE backend — core dependencies
# Generated 2026-04-13
#
# This file mirrors backend/requirements.txt exactly. Railway's build runs
# `pip install -r requirements.txt` from the repo root (before `cd backend`
# in the start command), so this root copy — not backend/requirements.txt —
# is what actually gets installed into the deployed container. It had
# drifted out of sync: missing pyjwt (breaking Supabase JWT auth at import
# time — ModuleNotFoundError: No module named 'jwt'), missing redis and
# boto3, and an impossible "starlette>=1.0.0" constraint (starlette has
# never released a 1.0 — see https://pypi.org/project/starlette/#history).
# Keep this file and backend/requirements.txt identical.

# Web framework
starlette>=0.40.0,<1.0.0
fastapi>=0.135.0
uvicorn[standard]>=0.30.0

# HTTP
requests>=2.33.0
httpx>=0.28.0
curl_cffi>=0.15.0

# SSL (macOS native trust store support)
truststore>=0.10.0

# Database
sqlalchemy>=2.0.0
alembic>=1.18.0
psycopg2-binary>=2.9.0

# Pydantic / validation
pydantic>=2.13.0
pydantic-settings>=2.13.0
annotated-types>=0.7.0

# HTML parsing
beautifulsoup4>=4.14.0

# Fuzzy matching
rapidfuzz>=3.6.0

# Scheduling
apscheduler>=3.10.0

# Auth (Supabase JWT verification)
pyjwt>=2.8.0

# Cache
redis>=5.0.0

# Utilities
python-dotenv>=1.2.0

# Object storage (Cloudflare R2 image uploads)
boto3>=1.34.0
