from __future__ import annotations

# DEPRECATED / UNUSED: leftover from the old app/pipeline/* candidate
# pipeline, superseded by app.services.discovery.pipeline_v2. This file was
# already empty (0 bytes) when audited — a grep across the entire backend/
# tree found zero imports of app.pipeline.crawl_scheduler anywhere.
# Kept here for reference only — not wired up. Safe to delete if no longer needed.
