# CRAVE backend — optional / development dependencies
#
# Install with:  pip install -r requirements-dev.txt

-r requirements.txt

# Testing
pytest>=9.1.1

# playwright and pdfminer.six moved to requirements.txt — both are
# imported at module level on the prod menu-extraction escalation path
# (see the comment there), so declaring them dev-only was itself the bug:
# it meant that entire tier silently never ran in production.
